# --- CONFIGURAÇÕES DO SERVIDOR ---
# Define a porta em que a aplicação vai rodar
server.port=8080

# --- CONFIGURAÇÕES DO BANCO DE DADOS H2 ---
# Habilita o console web do H2
spring.h2.console.enabled=true
# Define o caminho para acessar o console (ex: http://localhost:8080/h2-console)
spring.h2.console.path=/h2-console

# Configurações de conexão do banco de dados
spring.datasource.url=jdbc:h2:mem:bibliotecaDB
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- CONFIGURAÇÕES DO JPA / HIBERNATE ---
# Define a plataforma do banco de dados para o JPA
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Atualiza o esquema do banco de dados automaticamente
spring.jpa.hibernate.ddl-auto=update
# Mostra os comandos SQL executados no console
spring.jpa.show-sql=true